# ============================================================================
# GenAI DataOps Agent - Environment Variables Template
# ============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env
#
# All variables marked with [REQUIRED] must be set for the agent to run.
# Database configuration: Use Option A (connection string) OR Option B (modular fields)
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: OpenAI API Configuration
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ----------------------------------------------------------------------------
# REQUIRED: Slack Bot Configuration
# ----------------------------------------------------------------------------
# Get these from your Slack App settings: https://api.slack.com/apps
# 
# SLACK_BOT_TOKEN: Bot User OAuth Token (starts with xoxb-)
#   Found in: OAuth & Permissions → Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token-here

# SLACK_SIGNING_SECRET: Signing Secret for request verification
#   Found in: Basic Information → App Credentials → Signing Secret
SLACK_SIGNING_SECRET=your-slack-signing-secret-here

# SLACK_APP_TOKEN: App-Level Token for Socket Mode (starts with xapp-)
#   Found in: Basic Information → App-Level Tokens → Create Token
#   Required scopes: connections:write
SLACK_APP_TOKEN=xapp-your-slack-app-token-here

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION (Choose ONE option)
# ----------------------------------------------------------------------------

# Option A: Full Connection String (Recommended for production)
# Format: postgresql://user:password@host:port/database
# Example: postgresql://myuser:mypass@localhost:5432/mydb
DB_CONNECTION_STRING=postgresql://user:password@host:port/database

# Option B: Modular Database Fields (Alternative to connection string)
# Use these if you prefer separate environment variables
# All 5 fields must be set if using Option B
# DB_USER=your-database-user
# DB_PASS=your-database-password
# DB_HOST=your-database-host
# DB_PORT=5432
# DB_NAME=your-database-name

# ----------------------------------------------------------------------------
# OPTIONAL: MCP Server Configuration (Future use)
# ----------------------------------------------------------------------------
# MCP_SERVER_URL=http://localhost:8000/mcp

# ----------------------------------------------------------------------------
# NOTES:
# ----------------------------------------------------------------------------
# - Never commit your actual .env file to version control
# - Keep your API keys and tokens secure
# - For local development, use Option B (modular DB fields) for clarity
# - For production/cloud, use Option A (connection string) for security
# ============================================================================